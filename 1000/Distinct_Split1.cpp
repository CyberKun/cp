#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;

    while (t--)
    {
        int n, flag = 0, pair = 0;
        cin >> n;

        vector<char> str(n);
        vector<int> str1, str2;

        for (int i = 0; i < n; i++)
            cin >> str[i];


    }

    return 0;
}
